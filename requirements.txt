# Runtime — Day 1 only; heavier deps land on the day they're first used
fastapi==0.115.12
uvicorn[standard]==0.34.2
pydantic==2.11.4
pydantic-settings==2.9.1
structlog==25.3.0
python-dotenv==1.1.0

# Pulled forward from Day 3 — used by playwright_playground/ for training
playwright==1.49.1

# Pulled forward from Day 2 — used by the real-router Playwright probe and the
# upcoming cli_agent/connection.py. SSH transport to Cisco IOS XE devices.
netmiko==4.4.0
ntc-templates==9.1.0

# Day 4 — Anthropic SDK for the orchestrator/planner tool-use loop.
anthropic==0.101.0

# Day 4 (WebUI scaffolds) — selectors loaded from YAML files.
pyyaml==6.0.2

# Day 6 — RAG (knowledge_agent): chunk PDFs, embed via MiniLM, persist to Chroma.
# torch is a transitive dep of sentence-transformers; force CPU build to skip
# the multi-GB CUDA wheels:
#   pip install --index-url https://download.pytorch.org/whl/cpu torch
# THEN: pip install -r requirements.txt
sentence-transformers==5.5.0
chromadb==1.5.9
pypdf==6.11.0

# Dev / test
ruff==0.11.10
mypy==1.14.1
pytest==8.3.5
pytest-asyncio==0.26.0
pytest-playwright==0.5.1
httpx==0.28.1
pre-commit==4.2.0
